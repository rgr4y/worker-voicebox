name: Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Clear space to remove unused folders
        run: |
          rm -rf /usr/share/dotnet
          rm -rf /opt/ghc
          rm -rf "/usr/local/share/boost"
          rm -rf "$AGENT_TOOLSDIRECTORY"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set environment variables
        run: |
          echo "HUGGINGFACE_ACCESS_TOKEN=${{ secrets.HUGGINGFACE_ACCESS_TOKEN }}" >> $GITHUB_ENV
          SHORT_SHA=$(echo "${{ github.sha }}" | cut -c1-7)
          echo "RELEASE_VERSION=${SHORT_SHA}" >> $GITHUB_ENV

      - name: Build and push
        uses: docker/bake-action@v2
        with:
          push: true
          set: |
            voicebox.tags=dx4100/worker-voicebox:${{ env.RELEASE_VERSION }},dx4100/worker-voicebox:latest
            voicebox.platform=linux/amd64
            *.args.RELEASE_VERSION=${{ env.RELEASE_VERSION }}
            *.args.HUGGINGFACE_ACCESS_TOKEN=${{ env.HUGGINGFACE_ACCESS_TOKEN }}

      - name: Build Summary
        run: |
          echo "Build completed"
          echo "Version: ${{ env.RELEASE_VERSION }}"
          echo "Image: dx4100/worker-voicebox:${{ env.RELEASE_VERSION }}"
